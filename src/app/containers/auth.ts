import { Component } from '@angular/core';

@Component({
  selector: 'auth-container',
  template: `
    <div class="auth-container row center-xs middle-xs">
      <h1>auth</h1>
    </div>
  `
})
export class Auth {}
